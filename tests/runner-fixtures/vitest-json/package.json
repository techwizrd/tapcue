{
  "name": "tapcue-vitest-fixture",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test:json": "vitest run --reporter=json"
  },
  "devDependencies": {
    "vitest": "^4.0.18"
  }
}
